apiVersion: v1
kind: Service
metadata:
  name: consumer
  labels:
    app: consumer
spec:
  selector:
    app: consumer
  ports:
    - port: {{ .Values.consumer.port }}
      targetPort: {{ .Values.consumer.port }}

